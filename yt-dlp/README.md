# yt-dlp by fralid V.2.0

Простой графический интерфейс для скачивания видео с YouTube с использованием `yt-dlp` и аппаратного ускорения NVIDIA (NVENC).

---

## 🆕 Что нового в V.2.0

### Исправления
- **Устранена ошибка "n challenge solving failed"** — добавлена поддержка Node.js для обхода JS-защиты YouTube
- **Исправлена ошибка "Conversion failed"** — корректная передача аргументов ffmpeg для перекодировки
- **Поддержка 10-bit видео** — добавлен `-pix_fmt yuv420p` для совместимости с NVENC

### Улучшения
- **Максимальное качество** — теперь скачивается лучшее доступное качество (включая WebM/VP9), а не только MP4
- **Nightly-версия yt-dlp** — скрипт `update.bat` теперь обновляет до nightly-сборки для актуальных фиксов
- **Оптимизация конвейера** — добавлен `--merge-output-format mp4` для прямого объединения в MP4

### Новые зависимости
- **`node.exe`** — должен лежать в папке со скриптом для решения JS-челленджей YouTube

---

## 📋 Требования

Данный скрипт предназначен для работы в среде Windows и требует наличия видеокарты NVIDIA для кодирования.

### Программное обеспечение (в папке со скриптом)
Для работы программы в текущей директории **обязательно** должны находиться следующие файлы:
1.  **`yt-dlp.exe`** — исполняемый файл загрузчика
2.  **`ffmpeg.exe`** — утилита для обработки видео
3.  **`node.exe`** — JavaScript runtime для обхода защиты YouTube *(новое в V.2.0)*
4.  **`www.youtube.com_cookies.txt`** — файл с куки для доступа к видео

### Python библиотеки
```bash
pip install gradio
```

## 🍪 Инструкция по получению Cookies

Для избежания блокировок и доступа к видео (включая 18+) необходимо использовать файл с cookies.

1.  Установите расширение для браузера **Get cookies.txt LOCALLY**:
    [Ссылка на Chrome Web Store](https://chromewebstore.google.com/detail/get-cookiestxt-locally/cclelndahbckbenkjhflpdbgdldlbecc)
2.  Зайдите на сайт [YouTube.com](https://www.youtube.com/) и войдите в свой аккаунт.
3.  Нажмите на иконку расширения.
4.  Скачайте файл с куки.
5.  **Переименуйте** скачанный файл в `www.youtube.com_cookies.txt`, если он назван иначе.
6.  Поместите этот файл в папку со скриптом `yt-dlp.py`.

> ⚠️ **Важно:** Куки могут устаревать. Если появляется ошибка "Sign in to confirm you're not a bot", обновите файл куки.

## 🚀 Как использовать

1.  Запустите скрипт `yt-dlp.py`
2.  После запуска в консоли появится ссылка (обычно `http://127.0.0.1:7860`), откройте её в браузере
3.  Вставьте ссылки на видео в текстовое поле (по одной ссылке на строку)
4.  Нажмите кнопку "Submit"
5.  Видео будут скачаны в папку `save/`, которая создается автоматически

## ⚙️ Особенности работы
- **Качество**: Скачивается лучшее доступное видео (любой формат), затем перекодируется в MP4
- **Кодирование**: `h264_nvenc` (NVIDIA) — preset p4, CQ 19, AAC 128k
- **Цветовой профиль**: Принудительный `yuv420p` для совместимости

## 🔄 Как обновить

YouTube часто меняет алгоритмы, поэтому `yt-dlp` может перестать работать.

Для обновления просто запустите `update.bat` — он автоматически скачает **nightly-версию** с GitHub (содержит самые свежие исправления).