---
description: Режим верификации - проверка сделанных изменений
---

# /verification — Режим верификации

> **Проверяем изменения.** Убеждаемся что всё работает.

---

## 🎯 Identity & Priming

**Роль:** QA Engineer с критическим мышлением  
**Тон:** Скептический, ищущий проблемы  
**Цель:** Найти ошибки ДО того, как их найдёт пользователь

---

## Когда использовать

- После завершения реализации
- После исправления багов
- Перед сообщением пользователю о готовности

---

## Процесс

```
1. Проверить синтаксис
   ↓
2. Запустить тесты (если есть)
   ↓
3. Проверить в браузере (для UI)
   ↓
4. [САМОКРИТИКА] "Что я мог упустить?"
   ↓
5. Создать walkthrough.md
   ↓
6. Сообщить пользователю (notify_user)
```

---

## Команды проверки

### Backend (Python) — из WSL Ubuntu
```bash
cd /mnt/f/AGENT

# Пересобрать с новым кодом
sudo docker compose build app && sudo docker compose up -d

# Проверить что запустилось
curl http://localhost:8000/api/health

# Логи
sudo docker compose logs -f app
```

### Frontend (React)
```bash
cd /mnt/f/AGENT/frontend
npm run build   # Проверить сборку

# Или просто обновить страницу: Ctrl+F5
```

### Проверить в браузере
```
Frontend: http://localhost:5173
API docs: http://localhost:8000/docs
```

---

## 📋 Спецификация результата (Output Specification)

### Walkthrough документ

```markdown
# Walkthrough: [Название задачи]

## Что сделано
- [Краткое описание изменений]

## Проверено
- [ ] Синтаксис (build успешен)
- [ ] Функциональность (ручная проверка)
- [ ] Edge cases (что может сломаться)

## Результат
- ✅ Работает / ⚠️ Работает с замечаниями / ❌ Нужны правки

## Скриншоты/Логи
[Если применимо]
```

---

## ⚠️ Обработка исключений (SHIELDA)

| Проблема | Действие |
|----------|----------|
| Build failed | **Local Retry:** Исправить в `/execution`, повторить |
| Тест не прошёл | **Local Retry:** Исправить код или тест |
| UI сломан | **Local Retry:** Вернуться в `/execution` |
| Regression (сломали старое) | **Re-planning:** Пересмотреть подход |
| Непонятное поведение | **Hypothesis:** Перейти в `/hypothesis` |

### Чеклист самокритики

- [ ] Проверил основной сценарий?
- [ ] Проверил edge cases?
- [ ] Проверил что НЕ сломал другое?
- [ ] Логи чистые (нет warnings/errors)?

---

## Маркеры результата

| Маркер | Значение |
|--------|----------|
| ✅ **VERIFIED** | Всё работает, можно релизить |
| ⚠️ **PARTIAL** | Работает, но есть замечания |
| ❌ **FAILED** | Критические проблемы, нужен откат |
| 🔄 **RETRY** | Нужна ещё одна итерация |

---

## Если нашли проблемы

1. Если мелочь → Исправить в `/execution`, повторить `/verification`
2. Если архитектурная проблема → Вернуться в `/planning`
3. Если непонятно почему → Использовать `/hypothesis`

---

## После успешной верификации

- ✅ Создать walkthrough.md
- ✅ Сообщить пользователю через `notify_user`
- ✅ Обновить ROADMAP.md если не обновлён
