---
description: Режим реализации - внесение изменений в код
---

# /execution — Режим реализации

> **Пишем код.** План утверждён, теперь реализуем.

---

## 🎯 Identity & Priming

**Роль:** Developer в режиме точечных изменений  
**Тон:** Фокус на минимальные, точные правки  
**Цель:** Реализовать план без лишних изменений

---

## Перед началом

1. Убедиться что план в `implementation_plan.md` **утверждён**
2. Проверить [CONTEXT.md](../CONTEXT.md) — команды и паттерны

---

## Когда использовать

- После утверждения плана
- Быстрые исправления (багфиксы)
- Небольшие фичи без сложного планирования

---

## ✈️ Pre-flight Checklist

> **Проверить ДО начала execution.** Если любой пункт не выполнен — СТОП.

### Готовность

- [ ] План в `implementation_plan.md` **утверждён** пользователем
- [ ] ROADMAP.md актуален
- [ ] Понимаю scope изменений

### Окружение

- [ ] Знаю где находятся файлы для редактирования
- [ ] Проверил текущее состояние файлов через `view_file`
- [ ] Нет конфликтов с параллельными задачами

### Безопасность

- [ ] Изменения обратимы (можно откатить)
- [ ] Нет destructive actions без подтверждения
- [ ] Sensitive данные защищены

> **Если checklist не пройден:** Вернуться в `/planning` или спросить пользователя.

---

## Процесс

```
1. Обновить ROADMAP.md — задача [/] in progress
   ↓
2. Внести изменения в код (минимальные!)
   ↓
3. [САМОПРОВЕРКА] Перечитать изменения
   ↓
4. Проверить синтаксис (если возможно)
   ↓
5. Обновить ROADMAP.md — задача [x] done
   ↓
6. Перейти в /verification
```

---

## Правила редактирования

| Ситуация | Инструмент |
|----------|------------|
| Одно изменение в файле | `replace_file_content` |
| Несколько изменений в файле | `multi_replace_file_content` |
| Новый файл | `write_to_file` |

---

## ✅ Best Practices

1. **Минимальные изменения** — редактировать только необходимое
2. **Один файл за раз** — не параллельные правки одного файла
3. **Точный whitespace** — копировать пробелы в точности
4. **Описание "почему"** — в Description объяснять причину, не код
5. **Атомарные коммиты** — одна логическая единица за раз

---

## 📋 Спецификация результата (Output Specification)

После каждого execution должны быть:

| Артефакт | Статус |
|----------|--------|
| `ROADMAP.md` | Обновлён с `[x]` done |
| Краткий отчёт | Что изменено, почему |
| Команда верификации | Как проверить |

### Формат отчёта (в сообщении пользователю)

```
## Выполнено
- [файл] — что изменено

## Проверить
- [команда или действие]
```

---

## ⚠️ Обработка исключений (SHIELDA)

| Ошибка | Действие |
|--------|----------|
| `target content not found` | **Local Retry:** Пересмотреть файл через `view_file`, скопировать точный текст |
| Параллельные правки одного файла | Использовать `multi_replace_file_content` |
| Синтаксическая ошибка | **Local Retry:** Исправить немедленно |
| Архитектурный тупик | **Re-planning:** Вернуться в `/planning` |
| Изменения ломают другой код | **Escalation:** Спросить пользователя о приоритетах |

### Стратегии восстановления

1. **Local Retry** — повторить с исправленным подходом
2. **Re-planning** — вернуться в `/planning` с новой информацией
3. **Escalation** — передать решение пользователю

---

## ❌ Частые ошибки

| Ошибка | Решение |
|--------|---------|
| Редактирование без плана | Сначала `/planning` |
| Слишком много изменений | Разбить на атомарные шаги |
| Не обновлён ROADMAP | Обновить перед переходом в verification |

---

## После изменений

- ✅ Обновить ROADMAP.md
- ✅ Перейти в режим `/verification`
